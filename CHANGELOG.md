# Changelog

## LP-24 — Navigation globale & metadata des pages sans contenu propre

- **La navbar était muette sur 294 des 298 pages.** Ses liens étaient des ancres nues (`#services`, `#pricing`, `#about`, `#contact`) : rendus par le layout racine, donc présents sur toutes les pages ville/département/aéroport/gare, où ces ancres ne pointent vers rien. Passés en liens absolus (`/#services`…) via `next/link` — retour à l'accueil puis défilement depuis une page SEO, simple défilement depuis l'accueil (sans rechargement).
- **La marque « Soualiho VTC » était un `<span>`, pas un lien.** Depuis une page SEO, l'en-tête n'offrait aucun retour vers l'accueil. Devenue un `<Link href="/">`.
- **Aucun décalage d'ancre.** Le header est `sticky top-0` sur 4rem et il n'existait ni `scroll-padding-top` ni `scroll-margin` : même sur l'accueil, un saut vers `#pricing` plaçait le titre visé *sous* le header. Ajout de `html { scroll-padding-top: 5rem }` (`app/globals.css`). `scroll-behavior: smooth` a été essayé puis **retiré** : sur une navigation inter-pages, l'animation fait défiler toute la page d'accueil pendant plusieurs secondes avant d'atteindre l'ancre.
- **Nouvelle entrée « Zones » → `/vtc`** dans l'en-tête : les ~290 pages SEO n'étaient atteignables que par le footer ou les moteurs de recherche. Le header reste un Server Component pur (pas d'`aria-current` via `usePathname`, qui imposerait `"use client"`). Espacement `gap-6 lg:gap-8` : mesuré, l'en-tête à 5 entrées demande 737 px au palier `md` (768 px) — il débordait de 1 px avec `gap-8` partout.
- **Page 404 dédiée** (`app/not-found.tsx`). Sans elle, Next servait son 404 par défaut : texte anglais (« 404: This page could not be found. ») et surtout **metadata héritée du layout** — la page 404 se décrivait avec le titre et la meta description de l'accueil. Désormais : titre et description propres, `robots: noindex, follow`, contenu français, CTA appel/WhatsApp et liens de secours vers les 4 hubs.
- **Canonical de l'accueil** (`app/page.tsx`) : seule page du site à ne pas en déclarer une. Posée sur la page et non sur le layout — au niveau layout, elle serait héritée par la 404, qui pointerait alors vers l'accueil.
- **Audit meta description** : contrairement à l'hypothèse de départ, les 298 pages réelles en avaient déjà une. Le seul défaut était celui de la 404 ci-dessus (`_global-error.html`, la frontière d'erreur interne de Next, n'en a pas — elle est rendue hors layout et n'est jamais explorée).
- **Correction d'accord (défaut introduit en LP-21→23)** : les pages département écrivaient « VTC dans le Essonne », « les communes du Yvelines ». Le genre et le nombre varient d'un département à l'autre ; deux tables dans `lib/constants.ts` (`IDF_DEPARTEMENT_LE`, `IDF_DEPARTEMENT_DE`) et deux helpers (`departementLe`, `departementDe`) couvrent les tournures utilisées. Les 7 `<h1>` et les 7 meta descriptions sont corrigés (« dans l'Essonne », « dans les Yvelines », « dans la Seine-et-Marne », « du Val-de-Marne », « de l'Essonne »…).
- Tests : `lib/communes.test.ts` étendu (4 tests sur l'accord des articles, dont l'élision et le repli sur code inconnu).
- Vérifié : `npm run lint`/`npm test` (142/142)/`npm run build` (298 pages) passent. **Sur le build de production** en navigateur : navigation `/vtc/departement/essonne` → « Services » → `/#services` (titre visible sous le header), `/vtc/gare/paris-montparnasse` → « Contact » → `/#contact`, ancre sur la page courante (titre à 252 px, bas du header à 65 px), « Zones » → `/vtc` depuis la 404, lien marque → accueil, rendu de la page 404. Largeur d'en-tête au palier `md` mesurée à 737 px (31 px de marge).
- **⚠️ Non vérifié** : le rendu à 768 px et en dessous n'a pas pu être capturé visuellement — `resize_window` ne change pas `window.innerWidth` dans le navigateur automatisé de cette session (même limite qu'en LP-18) ; la conclusion repose sur une mesure DOM des largeurs, pas sur une capture.
- **Hors périmètre, à trancher** : il n'y a **aucune navigation sur mobile** (`hidden md:flex`) — situation préexistante, valable aussi sur l'accueil. Le lien de la marque est désormais le seul point de retour mobile. Un menu déroulant mobile serait un choix de design à confirmer.

## LP-21 → LP-23 — SEO programmatique : pages aéroport, gare et département (+ déblocage LP-19)

- **Déblocage LP-19 (le point clé)** : l'enrichissement des communes appelait ORS **Directions une fois par trajet** (266 communes × 4 destinations ≈ 1 064 requêtes), ce qui épuisait le quota gratuit en cours de route — d'où les 36 puis 84 communes des runs précédents. Bascule sur l'endpoint **Matrix** (`getMatrix`, `lib/ors.ts`), qui calcule N×M trajets en **une** requête : les 182 communes manquantes ont été enrichies en **5 requêtes**, et les 9 gares en **1**. Vérifié sur des trajets réels que Matrix renvoie exactement les mêmes distances/durées que Directions (même graphe de routage) — Paris→CDG : 27 274 m / 2 199,8 s dans les deux cas. `data/communes.json` est désormais **complet : 266/266 communes**, maillage `nearby` compris.
- **Nouvelles familles de pages** (toutes en Server Components purs, SSG, aucun JS applicatif) :
  - **Aéroport** — `app/vtc/aeroport/[aeroport]/page.tsx` (3 pages : Orly, Roissy-CDG, Beauvais-Tillé) + hub `app/vtc/aeroport/page.tsx`. Contenu unique : tarif fixe, terminaux, présentation factuelle, et surtout **deux tableaux de durées/distances réelles** (communes en zone tarif fixe / hors zone), triés par temps de trajet. Données de référence dans `data/aeroports.ts` (faits publics : implantation, terminaux, code IATA).
  - **Gare** — `app/vtc/gare/[gare]/page.tsx` (9 pages : les 7 grandes gares parisiennes + Massy TGV + Marne-la-Vallée Chessy) + hub `app/vtc/gare/page.tsx`. Contenu unique : destinations desservies, durées vers les 3 aéroports, et **table des correspondances de gare à gare** (calculées dans les deux sens, les sens uniques rendant aller et retour asymétriques).
  - **Département** — `app/vtc/departement/[departement]/page.tsx` (7 pages) + hub `app/vtc/departement/page.tsx`. Agrégats réels par département (communes desservies, population, durées moyennes vers Paris et les aéroports, zone tarifaire) + table commune par commune. **Paris (75) est volontairement exclu** : avec une seule commune couverte, la page ferait doublon avec `/vtc/paris` — c'est exactement le contenu mince que Google traite en *doorway page*. Le hub renvoie alors directement vers la page ville.
- **Pipeline de données gares** (one-off, jamais au build/CI) : `scripts/fetch-gares.ts` → `data/gares.raw.json` (coordonnées officielles issues du jeu **SNCF Open Data « gares-de-voyageurs »**, sur le modèle de geo.api.gouv.fr pour les communes ; le géocodage MapTiler s'est révélé inutilisable sur les noms de gare — il renvoyait « Quartier de la Gare » pour trois gares différentes) puis `scripts/enrich-gares.ts` → `data/gares.json`. Nouveaux scripts npm `data:fetch-gares`/`data:enrich-gares`. Schémas Zod dans `schemas/gare.ts`.
- **JSON-LD repensé** : chaque page émettait un second `LocalBusiness` avec un `areaServed` différent, soit **deux entités contradictoires** pour la même entreprise. Modèle corrigé — le layout décrit l'entreprise **une fois** avec un `@id` stable (`/#business`), et chaque page programmatique émet un **`Service`** rattaché via `provider: {"@id": …}`, portant sa propre zone (`City`/`AdministrativeArea`/`Place`) et, uniquement là où il s'applique, ses `Offer` de tarif fixe. Ajout d'un `BreadcrumbList` (avec fil d'Ariane visible) et d'une `FAQPage` sur toutes les pages programmatiques.
- **Nouveau code partagé** : `lib/communes.ts` (helpers purs : `slugify`, `groupByDepartement`, `fastestTo`, `averageMinutes`, `departementStats`, `departementSlug`…), `lib/jsonld.ts` (constructeurs schema.org), `components/seo/*` (`Breadcrumb`, `CtaButtons`/`CtaCall`, `CommuneTable`, `CommuneLinks`), `scripts/ors-throttle.ts` (Matrix par lots, dichotomie sur lot en échec, repli Directions à rayon élargi pour un point non routable) et `scripts/nearby.ts`.
- **`scripts/link-communes.ts`** (`npm run data:link-communes`) : recalcule le maillage `nearby` **sans toucher à ORS** — le calcul est purement géométrique, il n'avait aucune raison d'être prisonnier d'un run d'enrichissement interrompu.
- **`scripts/enrich-communes.ts`** : accepte `-- --departements=94,92,93` pour prioriser une zone quand le quota est contraint.
- **Maillage interne** : le footer liste désormais les 4 hubs (zones, département, aéroport, gare) sur toutes les pages ; les pages ville renvoient vers leur département et les pages aéroport ; `app/sitemap.ts` couvre les 290 URLs.
- Tests : `lib/communes.test.ts` (22, nouveau), `lib/jsonld.test.ts` (9, nouveau), `lib/ors.test.ts` étendu (4 tests `getMatrix` : indexation sources/destinations, conversions + correction de durée, case `null` non transformée en 0, rejet d'une réponse hors schéma).
- Vérifié : `npm run lint`/`npm test` (138/138)/`npm run build` passent — **298 pages statiques** générées (266 villes + 7 départements + 3 aéroports + 9 gares + hubs), contre 38 auparavant. Contrôle du HTML produit : un seul `<h1>` par page, `canonical` correct, un `LocalBusiness` + `Service`/`BreadcrumbList`/`FAQPage` par page. Vérification visuelle en navigateur des pages Orly, Val-de-Marne et Gare du Nord (rendu conforme aux tokens LP-18, tableaux de données réelles, CTA appel/WhatsApp).
- **⚠️ À faire avant mise en ligne** : `NEXT_PUBLIC_SITE_URL` vaut encore `https://your-domain.example` en local — les `canonical`, l'OpenGraph et le sitemap en dépendent.

## LP-20 — Migration formulaire de contact : Formspree → Resend

- **Contexte** : le formulaire de contact (LP-14) postait directement du navigateur vers Formspree via un identifiant de formulaire **public** (`NEXT_PUBLIC_FORMSPREE_FORM_ID`, pas un secret). Resend authentifie ses appels par une clé API **secrète** (`RESEND_API_KEY`) — la même contrainte que pour ORS (ADR-0001) s'applique donc : l'appel doit transiter par un Route Handler serveur. Décision documentée dans `docs/adr/0003-proxy-resend-route-handler.md`.
- **Nouveau** : `app/api/contact/route.ts` (Route Handler proxy, calqué sur `app/api/route/route.ts` — parse JSON défensif, validation `contactSchema` réutilisée telle quelle, 400 sur entrée invalide, 502 générique sur échec Resend, jamais l'erreur brute exposée) ; `lib/resend.ts` (client Resend serveur, `sendContactNotification()` + `formatContactEmailBody()`, instanciation du SDK différée pour ne pas casser `npm run build` quand `RESEND_API_KEY` est absente à la compilation — contrairement à `ORS_API_KEY`, le SDK Resend valide la clé dès son constructeur).
- **Modifié** : `lib/contact.ts` poste désormais vers `internalApi`/`api/contact` (chemin relatif) au lieu de l'URL Formspree absolue ; `lib/ky.ts` — suppression de l'instance `formspreeApi`, devenue inutile ; `components/sections/contact.tsx` — mention RGPD réécrite (ne nomme plus « Formspree »), aucun autre changement (champs, `react-hook-form`/Zod, gestion `isPending`/`isSuccess`/`isError` strictement identiques).
- **Nouvelles variables d'env** (serveur uniquement, jamais `NEXT_PUBLIC_`) : `RESEND_API_KEY`, `RESEND_FROM_EMAIL` (domaine vérifié en prod, sandbox `onboarding@resend.dev` en dev) — remplacent `NEXT_PUBLIC_FORMSPREE_FORM_ID` dans `.env.local.example`, `README.md`, `SETUP-ENV.md`.
- Nouvelle dépendance : `resend`.
- Tests : `lib/resend.test.ts` (nouveau, TDD sur `formatContactEmailBody` — champs optionnels absents/présents — et `sendContactNotification` avec SDK Resend mocké) ; `app/api/contact/route.test.ts` (nouveau, 200/400×3/502, sur le modèle de `app/api/route/route.test.ts`, vérifie que l'erreur Resend brute n'est jamais exposée) ; `lib/contact.test.ts` mis à jour (mock `internalApi` au lieu de `formspreeApi`).
- Vérifié : `npm run lint`/`npm test` (56/56)/`npm run build` passent. Test manuel `curl` sur `/api/contact` (400 JSON invalide, 400 consentement manquant, 502 avec clé Resend placeholder — message générique confirmé sans fuite). Test manuel navigateur : soumission du formulaire avec clé Resend placeholder → `502` confirmé dans le réseau, UI affiche bien le repli CTA téléphone (« Appeler pour réserver — 0673559197 »), comportement identique à l'ancien Formspree. Vérifié en navigateur que `RESEND_API_KEY` n'apparaît ni dans le HTML ni dans le bundle JS client.
- **⚠️ Non vérifié** : l'envoi réel d'un email via un vrai compte Resend (aucune clé `RESEND_API_KEY` valide disponible pendant l'implémentation — seule la logique côté serveur est validée par tests unitaires avec le SDK mocké). À confirmer par l'utilisateur avec une vraie clé (`SETUP-ENV.md` § 3) avant mise en production.

## LP-19 — SEO programmatique : pages ville Île-de-France (SSG)

- **Nouveau** : une page statique par commune IDF > 10 000 habitants (`app/vtc/[ville]/page.tsx`, `generateStaticParams`/`dynamicParams = false`) avec contenu unique (distances/durées réelles ORS vers Orly/CDG/Beauvais/Paris), tarif fixe affiché uniquement si `inFixedZone` (départements 75/92/93/94), sinon estimation + repli CTA. Page hub `app/vtc/page.tsx` (communes groupées par département). `generateMetadata` + JSON-LD `LocalBusiness` (`areaServed`) par ville. `app/sitemap.ts` étendu (home + hub + villes). Lien discret « Nos zones d'intervention » ajouté au footer.
- **Pipeline de données** (one-off, jamais au build/CI) : `scripts/fetch-communes.ts` (geo.api.gouv.fr, IDF, filtre population > 10 000) → `scripts/enrich-communes.ts` (réutilise `getDirections` de `lib/ors.ts`, throttlé, sauvegarde incrémentale et reprenable par code INSEE, calcule `inFixedZone` et le maillage `nearby` par distance à vol d'oiseau). Nouveaux scripts npm `data:fetch-communes`/`data:enrich-communes`.
- **Infrastructure scripts** : `scripts/register-ts-paths.mjs` + `scripts/resolve-ts-paths-hook.mjs` — hook de résolution de module Node permettant à ces scripts TypeScript one-off d'importer le code applicatif via l'alias `@/` (comme le reste du repo), sans nouvelle dépendance (tsx/ts-node), en s'appuyant sur le support natif de TypeScript par Node ≥ 22.6.
- **`lib/ors.ts`** : `getDirections()` accepte désormais un 3ᵉ paramètre optionnel `{ radiuses }` (rétrocompatible, non utilisé par le simulateur de la home) — nécessaire pour certains centroïdes de commune isolés (ex. Fontainebleau, en forêt) que le rayon de rattachement par défaut d'ORS (350 m) ne peut pas router. 2 nouveaux tests dans `lib/ors.test.ts`.
- **`lib/constants.ts`** : nouvelle table `IDF_DEPARTEMENTS` (référence géographique stable, code → nom des 8 départements franciliens) utilisée par les pages ville/hub.
- **Découvertes en cours de route** (documentées dans les scripts) :
  - `AIRPORTS.ORLY.coord`/`BEAUVAIS.coord` (constantes de détection de rayon, `lib/pricing.ts`) ne sont **pas routables** par ORS (déjà rencontré pour Orly en LP-17) — des coordonnées de substitution vérifiées routables sont utilisées dans le script d'enrichissement uniquement (`AIRPORT_ROUTING_COORDS`), sans toucher aux constantes existantes.
  - Le plan gratuit ORS renvoie occasionnellement `403 "Quota exceeded"` **malgré un quota disponible** — problème documenté côté ORS (forum officiel, cas similaire persistant ~24h), pas un bug de notre code. Le script retente avec un palier d'attente croissant (15 s → 120 s) avant d'abandonner proprement (progrès déjà sauvegardé).
- **⚠️ Enrichissement partiel** : **36 communes sur 266** enrichies avec succès avant que l'incident ORS ci-dessus ne bloque la suite. `data/communes.json` (36 communes) et `data/communes.raw.json` (266 communes, source) sont committés. Le script est **reprenable** : relancer `npm run data:enrich-communes` une fois l'incident ORS résorbé complète automatiquement les 230 communes restantes (aucune commune déjà enrichie n'est re-appelée). Le maillage `nearby` (communes proches) n'est calculé qu'à la fin d'un run complet — actuellement vide pour les 36 communes déjà enrichies, sans casser l'affichage (section masquée si vide).
- Vérifié : `npm run lint`/`npm test` (47/47)/`npm run build` passent (36 pages ville + hub générées en SSG). Test manuel navigateur : page `inFixedZone` (Paris, tarifs fixes 50 €/65 €/120 € affichés) et page hors zone (Avon, estimation/sur devis) ; hub groupé par département ; 404 sur slug inconnu ; sitemap contient les 38 URLs (home + hub + 36 villes) ; lien footer fonctionnel. Non-régression confirmée : `/api/route` et le simulateur de la home inchangés.

## Fix — alignement/taille des icônes (suite LP-18)

- **Cause** : le package `material-symbols/outlined.css` déclare `.material-symbols-outlined` (`display: inline-block`, `font-size: 24px`, etc.) **hors `@layer`**, alors que toutes les classes Tailwind (`flex`, `text-base`, `text-lg`, `text-3xl`, `text-4xl`, `text-[120px]`…) vivent dans `@layer utilities`. Par construction CSS, une règle hors `@layer` gagne **toujours** face à une règle dans un layer, quel que soit l'ordre d'import — donc chaque fois qu'une classe de taille/alignement était posée sur le **même élément** que `material-symbols-outlined`, elle était silencieusement ignorée.
- **Symptôme repéré par l'utilisateur** : icônes mal alignées dans `services.tsx` et `contact.tsx` (le conteneur `flex items-center justify-center` portait aussi la classe `material-symbols-outlined`, donc `display:flex` perdait face à `display:inline-block`).
- **Portée réelle du bug** (trouvée en creusant) : au-delà de l'alignement, **toutes** les tailles d'icônes personnalisées (`text-base`, `text-lg`, `text-3xl`, `text-4xl`, `text-[120px]`) du restylage LP-18 étaient inopérantes — chaque icône retombait au 24px par défaut du package, y compris la grande icône décorative de `airport-pricing.tsx` censée faire 120px.
- **Correctif** :
  - `services.tsx` / `contact.tsx` (icônes dans une boîte `flex` centrée) : séparation en deux éléments — le conteneur garde `flex items-center justify-center` + les styles de boîte, l'icône (`material-symbols-outlined`, sans autre classe) vit dans un `<span>` imbriqué, qui n'entre donc plus en collision.
  - Partout ailleurs (icônes avec une taille custom sans conflit de `display`) : ajout du modifier Tailwind v4 `!` sur la classe de taille (ex. `text-base!`, `text-lg!`, `text-3xl!`, `text-4xl!`, `text-[120px]!`) — une déclaration `!important`, même dans un `@layer`, regagne la priorité face à une règle non importante hors `@layer`.
  - Fichiers concernés : `header.tsx`, `footer.tsx`, `sticky-call-button.tsx`, `hero-itinerary.tsx`, `airport-pricing.tsx`, `direct-booking.tsx`, `about.tsx`, `zones.tsx`, `reviews.tsx`, `contact.tsx`, `services.tsx`, `price-estimate.tsx`, `itinerary-simulator.tsx`, `address-autocomplete.tsx`.
- Vérifié : `npm run lint`/`npm test` (45/45)/`npm run build` passent ; tailles calculées confirmées dans le navigateur (`text-base!` → 16px, `text-lg!` → 18px, `text-[120px]!` → 120px, contre 24px partout avant le correctif) ; rendu visuel des sections Services et Contact confirmé aligné.

## LP-18 — Intégration CSS de la maquette Stitch « Midnight Elite »

- Restylage CSS/Tailwind intégral de toutes les sections (header, hero+simulateur, tarifs aéroport, « Pourquoi nous choisir » en grille bento, services, à propos, zones, avis, contact, footer, bouton d'appel flottant) d'après l'export exact `stitch_plateforme_vtc_professionnelle/code.html` + `DESIGN.md` — **aucune logique métier touchée** (confirmé : `lib/`, `schemas/`, `app/api/` inchangés, `npm test` 45/45 sans modification).
- Nouveaux tokens de thème dans `app/globals.css` (`@theme`) : couleurs (`primary` #000000, `accent`/or #bfa15a, `deep-midnight` #0f1115, `surface`/`surface-low`/`border`…), rayons (`rounded-standard`, `rounded-card`), ombre `ambient-shadow`/`hover-lift`.
- Polices Montserrat (headlines) + Inter (corps) via `next/font/google` (remplace Geist), package `material-symbols` (self-hosted, `outlined.css`) pour les icônes — remplace les emojis des CTA.
- `about.tsx` : 2 emplacements image vides (`role="img"`, `aspect-[4/3]`) pour la photo chauffeur et véhicule, dimensionnés d'après la maquette — toujours aucune image réelle codée en dur.
- `address-autocomplete.tsx` : nouvelle prop optionnelle `icon` (purement visuelle) pour afficher une icône Material Symbols dans les champs départ/destination du simulateur.
- **Exclusions délibérées** (contenu fabriqué présent dans l'export Stitch mais non repris) : bloc « preuve sociale » du hero (avatars + « 1000 clients satisfaits », chiffre inventé par l'IA) ; champs `date`/`heure` du formulaire hero (absents du simulateur réel — les ajouter aurait été une nouvelle fonctionnalité) ; liens footer vers des pages inexistantes (Privacy Policy, Terms, Legal Notice, Fleet).
- `direct-booking.tsx` : les 5 avantages existants (`ADVANTAGES`) reformatés en grille bento asymétrique reprenant la structure de la maquette, contenu inchangé.
- `contact.tsx` : mise en page « split » 2 colonnes (cartes de contact cliquables + carte formulaire), **les 7 champs et toute la logique `react-hook-form`/Zod/Formspree restent strictement identiques**.
- Nouvelle dépendance : `material-symbols` (icônes, self-hosted).
- Vérifié : `npm run lint`, `npm test` (45/45, aucun test modifié), `npm run build` passent. Rendu visuel comparé section par section à la maquette en desktop (1440px) via navigateur — très fidèle. **Rendu mobile ≤ 380px non vérifié visuellement** (limite de l'environnement de test : le viewport du navigateur automatisé est resté bloqué à 1470px malgré `resize_window`) ; le mobile-first a été vérifié par relecture de code (classes Tailwind non préfixées = mobile, `sm:`/`md:`/`lg:` pour le desktop, cohérent sur tout le diff) — à confirmer visuellement dans un vrai navigateur.

## LP-17 — Vérification & fiabilisation des estimations de durée de trajet

- **Mesure** (2026-09-02, PRP `PRPs/LP-17-verification-duree-trajet.md`) : comparaison réelle ORS (`/api/route`) vs Google Maps sur 5 trajets Île-de-France (`curl` réel, hors suite automatisée). Constat : ORS reste proche de Google Maps sur autoroute/longue distance (écart −3 % à −7 %), mais sous-estime fortement les trajets urbains courts (−31 % à −34 %) — cohérent avec l'absence de trafic temps réel dans le profil `driving-car` (vitesses moyennes statiques OSM). Deux mesures (banlieue→Paris, Paris→CDG) partiellement biaisées par des incidents temps réel ponctuels côté Google (fermeture de route, accident), non imputables à ORS.
- **Correctif** : `lib/constants.ts` — nouvelle constante `ROUTE_DURATION_CORRECTION` (facteur par palier de distance : ×1,5 < 8 km, ×1,1 entre 8 et 20 km, ×1,0 au-delà — pas de régression continue, seulement 3 points de mesure fiables). Appliquée dans `lib/ors.ts` (`correctDurationMin()`) juste après la conversion `s → min`, donc propagée automatiquement au prix affiché (`lib/pricing.ts` consomme `durationMin`) sans modification de la formule de pricing ni de l'UI.
- Tests : `lib/ors.test.ts` (TDD, 4 nouveaux cas couvrant les 3 paliers + la frontière à 8 km) ; `ai_docs/openrouteservice.md` mis à jour (nouvelle limite documentée dans « Limites & bonnes pratiques »).
- Vérifié : re-test réel des 5 trajets après correctif — l'urbain court (Gare de Lyon → Gare du Nord) passe de 18,4 à 27,6 min (Google : 28 min) ; L'Haÿ-les-Roses → Orly passe de 21,5 à 23,6 min (Google : 23 min). `npm run lint`/`npm test` (45/45)/`npm run build` passent.
- `docs/BACKLOG.md` : `LP-17` coché `[x]`.

## Fix — la carte MapLibre n'apparaît jamais (dev, Turbopack)

- Cause identifiée via la console navigateur de l'utilisateur : `Failed to load module script: The server responded with a non-JavaScript MIME type of "text/html"`. `maplibre-gl` v6 déduit l'URL de son Web Worker (module ES) via `import.meta.url` au runtime ; Turbopack (`next dev`) ne la résout pas en une URL `http(s)` exploitable, Next.js répond alors avec sa page de fallback HTML, et le chargement des tuiles échoue silencieusement (aucune erreur `map.on("error")`, `style.json`/`sprite`/`tiles.json` chargent normalement mais aucune requête `.pbf` n'est jamais émise).
- Correctif : l'URL du worker est désormais fixée explicitement via `setWorkerUrl("/maplibre-gl-worker.mjs")` dans `components/itinerary/route-map.tsx`, vers des assets statiques générés par `scripts/copy-maplibre-worker.mjs` (copie de `maplibre-gl-worker.mjs` **et** `maplibre-gl-shared.mjs` — importé en relatif par le worker — depuis `node_modules/maplibre-gl/dist/` vers `public/`), câblé en `predev`/`prebuild`/`postinstall` dans `package.json`. Les fichiers générés sont gitignorés et exclus du lint (`eslint.config.mjs`).
- `ai_docs/maplibre-maptiler.md` mis à jour (nouvelle section « ⚠️ Carte qui n'apparaît jamais (tuiles jamais chargées) sous Turbopack ») pour éviter la régression.
- Vérifié en conditions réelles (capture d'écran) : le worker et son import relatif se chargent avec `Content-Type: application/javascript` (200), les tuiles `.pbf` sont bien requêtées, et la carte s'affiche (routes, villes, aéroport CDG visibles). `npm run lint`/`npm test`/`npm run build` passent.

## Fix — 502 « Calcul d'itinéraire indisponible » sur `/api/route`

- Cause identifiée en local (log serveur temporaire) : `lib/ors.ts` appelait `https://api.openrouteservice.org/v2/directions/driving-car/geojson` sans header `Accept` explicite → ORS répondait **406 Not Acceptable** (le `Content-Type: application/json` posé automatiquement par `ky` via l'option `json` ne suffit pas), capturé par le `catch` générique du Route Handler et renvoyé en 502.
- Correctif : ajout de `Accept: "application/json, application/geo+json"` aux headers de la requête ORS dans `lib/ors.ts`.
- `ai_docs/openrouteservice.md` mis à jour (snippet + nouvelle section « ⚠️ Header `Accept` obligatoire ») pour éviter la régression.
- `lib/ors.test.ts` mis à jour (assertion sur les headers envoyés).
- Vérifié manuellement : `POST /api/route` avec des coordonnées réelles → 200 avec `geometry`/`distanceKm`/`durationMin` ; cas `400` (coordonnées invalides) toujours correct.

## LP-15 → LP-16 — SEO technique & audit responsive/accessibilité/performance (PRP `PRPs/LP-15-LP-16-seo-a11y-perf.md`)

- **LP-15** : `app/layout.tsx` — ajout de `metadataBase` (`NEXT_PUBLIC_SITE_URL`) et `openGraph` (title/description/url/siteName/locale/type) ; `<title>`, description et JSON-LD `LocalBusiness` déjà conformes depuis LP-03, revérifiés sans modification.
- `app/opengraph-image.tsx` — image OG générée dynamiquement via `ImageResponse` (nom + accroche depuis `BUSINESS`), aucune photo requise.
- `app/robots.ts` et `app/sitemap.ts` — fichiers spéciaux Next.js App Router, testés (`GET /robots.txt`, `GET /sitemap.xml` → 200).
- `NEXT_PUBLIC_SITE_URL` documentée dans `.env.local.example` et `README.md` (placeholder, aucun domaine inventé).
- **LP-16** — corrections issues de l'audit :
  - Focus clavier : remplacement de `outline-none` isolé par un anneau `focus-visible:ring-2` dans `components/sections/contact.tsx` et `components/itinerary/address-autocomplete.tsx` (vérifié réellement au clavier : `:focus-visible` actif, anneau visible).
  - Contraste : `text-zinc-500` en mode sombre remplacé par `text-zinc-400` dans `hero-itinerary.tsx`, `airport-pricing.tsx`, `reviews.tsx`, `contact.tsx` (calcul réel : 4.12:1 → 7.73:1) ; `footer.tsx` passé de `text-zinc-500` à `text-zinc-400` (3.69:1 → 6.92:1, fond `zinc-900` fixe) ; libellés « (optionnel) » du formulaire passés à `text-zinc-500 dark:text-zinc-400` (2.56:1 → 4.81:1 en mode clair).
  - Rendu ≤380px vérifié sans débordement horizontal (simulation via iframe 380px, la fenêtre Chrome ayant une largeur minimale ~735px empêchant un test direct).
  - Cibles tactiles mesurées réellement (30 éléments interactifs) : seuls les contrôles natifs MapLibre (zoom/rotation, 29px) et l'attribution légale (14px) sont sous 44px — non modifiables sans casser la bibliothèque tierce ou l'obligation de licence ; la case à cocher RGPD (20px visuel) a une cible effective de 282×120px via son `<label>`.
  - `cooperativeGestures` reconfirmé : un scroll sur la carte fait défiler la page plutôt que zoomer.
  - Audit Lighthouse (build de production, `npx lighthouse`) : **Accessibilité 100/100, SEO 100/100, Bonnes pratiques 96/100, Performance 68/100.**

## LP-08 → LP-14 — Sections de contenu (PRP `PRPs/LP-08-LP-14-sections-contenu.md`)

- Ajout des 6 sections statiques (`components/sections/{airport-pricing,direct-booking,services,zones,about,reviews}.tsx`) : Server Components, texte de `wording.md`, CTA `tel:`/`wa.me`, un seul `<h2>` chacune.
- LP-08 : tarifs Orly/CDG/Beauvais lus depuis `AIRPORT_FARES`/`AIRPORTS` (`lib/constants.ts`), aucun montant en dur.
- LP-11 : liste des gares desservies conservée telle quelle (« Marne-la-Vallée Chessy » à vérifier — note conservée en commentaire de code, pas affichée aux visiteurs).
- LP-12 : aucune photo du véhicule ajoutée (aucun asset fourni) ; emplacement réservé en commentaire.
- LP-13 : avis mappés depuis `REVIEWS` (déjà balisés « avis fictif » depuis LP-02) ; lien vers les avis Google affiché en texte non cliquable (aucune URL réelle n'existe).
- LP-14 : `components/sections/contact.tsx` (`"use client"`) — bloc coordonnées (`BUSINESS`), formulaire `react-hook-form` + `zodResolver` (`schemas/contact.ts`), soumission vers **Formspree** (`lib/contact.ts`, `lib/use-contact-form.ts`, instance `formspreeApi` dans `lib/ky.ts`), mention de consentement RGPD nommant explicitement Formspree, repli générique + CTA téléphone sur échec réseau.
- Nouvelles dépendances : `react-hook-form`, `@hookform/resolvers`.
- `NEXT_PUBLIC_FORMSPREE_FORM_ID` documentée dans `.env.local.example` et `README.md` (variable publique, comme `NEXT_PUBLIC_MAPTILER_KEY`) — placeholder tant que l'utilisateur n'a pas créé son compte Formspree.
- Wiring définitif dans `app/page.tsx` : `Hero → AirportPricing → DirectBooking → Services → Zones → About → Reviews → Contact`.
- Tests : `schemas/contact.test.ts` (6 tests, validation Zod) et `lib/contact.test.ts` (2 tests, Formspree mocké via `vi.mock("@/lib/ky")`).
- Écart voulu par rapport au PRP : `consentement` implémenté en `z.boolean().refine(...)` plutôt que `z.literal(true)` (incompatible avec une case à cocher décochée par défaut dans `react-hook-form`).

## LP-07 — Section Hero + assemblage du simulateur

- Ajout de `lib/ky.ts` (`internalApi`, instance ky client pour l'API interne) et `lib/use-route.ts` (hook `useRoute`, `useMutation` vers `POST /api/route`, réponse validée par `routeResponseSchema`).
- Ajout de `getFareEstimate` dans `lib/pricing.ts` : combine `getAirportFareOverride` (prioritaire) et `estimatePrice`, avec 2 tests dédiés (13/13 sur `pricing.test.ts`).
- Ajout de `components/itinerary/price-estimate.tsx` (panneau Distance/Durée/Estimation, mention « estimation indicative » ou tarif fixe aéroport, CTA appel + WhatsApp) et `components/itinerary/itinerary-simulator.tsx` (orchestrateur : 2 `AddressAutocomplete`, bouton « Calculer l'itinéraire » désactivé tant que les 2 points ne sont pas résolus, spinner sur `isPending`, carte, repli générique + CTA téléphone sur erreur).
- Ajout de `components/sections/hero-itinerary.tsx` (Server Component, unique `<h1>` de la page, sur-titre/accroche de `wording.md`, CTA appel/WhatsApp + micro-réassurance).
- Wiring définitif dans `app/page.tsx` (remplace le `return null` de LP-03/LP-06) : la page rend désormais `<HeroItinerary />`.
- Tests manuels réels (mock de `window.fetch` pour le géocodage et `/api/route`, dans un navigateur réel, faute de vraies clés API) : bouton désactivé → activé une fois les 2 adresses résolues, spinner + « Calcul en cours… » pendant `isPending`, panneau de résultat correct (« Distance : 10.0 km · Durée estimée : 15 min · Estimation : 37 € »), repli générique + CTA téléphone sur échec ORS réel (sans clé), `cooperativeGestures` actif sur la carte.

## LP-06 — Carte MapLibre & géocodage MapTiler

- Ajout de `schemas/itinerary.ts` : `geocodeFeatureSchema`, `geocodeSchema` (réponse MapTiler).
- Ajout de `lib/maptiler.ts` (client) : `geocode(query)`, biais Île-de-France, `country=fr`, court-circuit sous 3 caractères, réponse parsée avec `geocodeSchema`.
- Ajout de `components/itinerary/route-map.tsx` (MapLibre, tracé GeoJSON + `fitBounds`, `cooperativeGestures: true` pour ne pas piéger le scroll mobile) et `route-map-loader.tsx` (`dynamic(..., { ssr: false })` + squelette de chargement).
- Ajout de `components/itinerary/address-autocomplete.tsx` : combobox accessible (label, `role="combobox"`/`listbox`, clavier Entrée/Échap), debounce ~300 ms de la saisie, `useQuery` avec `enabled` sur la valeur débouncée.
- Ajout de `@types/geojson` en devDependency (dépendance transitive de `maplibre-gl` non hoistée par pnpm — nécessaire pour que le namespace `GeoJSON` soit résolu par `tsc`).
- Tests (`lib/maptiler.test.ts`, 4 tests) : géocodage mocké via `vi.mock("ky")`, court-circuit sous 3 caractères, biais de proximité, réponse non conforme rejetée.
- **Adaptation à la version installée de `maplibre-gl` (6.6.0)** : la doc `ai_docs/maplibre-maptiler.md` suppose un export par défaut (`import maplibregl from "maplibre-gl"`), mais cette version n'expose plus que des exports nommés (`Map` aliasé `MapLibreMap`, `NavigationControl`, `LngLatBounds`, …). Adapté en conséquence.

## LP-05 — Logique de tarification

- Ajout de `lib/pricing.ts` : `estimatePrice` (grille + minimum de course + majoration nuit/férié, arrondi à l'euro) et `getAirportFareOverride` (tarif fixe prioritaire si départ ou arrivée à proximité d'Orly/CDG/Beauvais).
- Ajout de `AIRPORTS` dans `lib/constants.ts` (coordonnées réelles des 3 aéroports, pas un placeholder) et de `Coord` dans `schemas/itinerary.ts` (type partagé `[lon, lat]`).
- TDD : table de cas limites (`lib/pricing.test.ts`, 11 tests) écrite et vérifiée en échec avant l'implémentation — minimum de course, arrondi à `.5`, majoration appliquée après le plancher, détection aéroport par rayon (~3 km), priorité de l'override sur l'estimation.

## LP-04 — Proxy OpenRouteService (Route Handler)

- Ajout de `schemas/itinerary.ts` : `coordSchema`, `routeRequestSchema`, `routeResponseSchema`, `orsDirectionsSchema`.
- Ajout de `lib/ors.ts` (`getDirections`, serveur uniquement) : appelle ORS Directions, parse la réponse avec `orsDirectionsSchema`, convertit mètres→km et secondes→min.
- Ajout de `app/api/route/route.ts` : `POST /api/route`, valide l'entrée et la sortie avec Zod, 400 sur JSON/coordonnées invalides, 502 avec message générique si ORS échoue (jamais l'erreur brute).
- Tests (`lib/ors.test.ts`, `app/api/route/route.test.ts`) : ORS mocké via `vi.mock` (aucun appel réseau réel), conversion d'unités, réponse ORS non conforme rejetée, 200/400/502 du Route Handler, non-fuite du message d'erreur externe.

## LP-03 — Layout, providers & shell SEO

- Ajout de `lib/query-client.tsx` (`Providers`, `QueryClientProvider` monté dans `app/layout.tsx`).
- Ajout de `components/layout/{header,footer,sticky-call-button}.tsx` : en-tête avec CTA appel (desktop), pied de page avec récap business + CTA appel/WhatsApp, bouton d'appel flottant fixe en bas d'écran sur mobile (`sm:hidden`).
- `app/layout.tsx` : `lang="fr"`, `<title>`/meta description conformes à `content-reference.md`, JSON-LD `LocalBusiness` (nom, téléphone, adresse, zone desservie, horaires 24/7, tarifs aéroport en `makesOffer`).
- Nettoyage du boilerplate `create-next-app` dans `app/page.tsx` (retrait du `<h1>` par défaut — le futur et unique `<h1>` sera celui du hero, LP-07) et suppression des SVG désormais inutilisés (`next.svg`, `vercel.svg`).

## LP-02 — Constantes business & placeholders

- Ajout de `lib/constants.ts` : `BUSINESS`, `AIRPORT_FARES`, `PRICING`, `REVIEWS`, et les helpers CTA `telHref`/`waHref`.
- Ajout de Vitest (config `vitest.config.mts`, script `pnpm test`) pour couvrir la normalisation de `telHref`/`waHref` (`lib/constants.test.ts`).
